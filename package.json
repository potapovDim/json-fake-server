{
  "name": "fake-server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf dist; tsc -d",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^3.3.0",
    "typescript": "^2.4.2"
  },
  "dependencies": {
    "koa-bodyparser": "^2.5.0",
    "koa-cors": "0.0.16",
    "koa-router": "^7.2.0",
    "koa2": "^2.0.0-alpha.7"
  }
}
